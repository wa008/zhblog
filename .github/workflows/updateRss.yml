name: updateRss

on:
  schedule:
    - cron: '5 * * * *'

jobs:
  build:
    # comment
    name: "add hackNews"
    # comment
    runs-on: ubuntu-latest

    steps:
      - name: Set up Python 3.9
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9

